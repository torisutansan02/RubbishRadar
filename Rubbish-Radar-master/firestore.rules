rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Anyone can read from `locations`, but only authenticated users can write
    match /locations/{locationId} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    // You can add other rules for users, reports, etc.
  }
}